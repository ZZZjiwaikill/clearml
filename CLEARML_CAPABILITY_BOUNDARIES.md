# ClearML 能力边界报告

## 1. 报告目标

这份文档不再以“怎么落地你当前业务流程”为主，而是以 **“ClearML 到底有哪些功能、每个功能能做什么、不能做什么、容易被误解成什么”** 为主。

换句话说，这是一份 **功能边界说明书**，而不是实施方案。

本文重点回答四类问题：

1. ClearML 的核心能力模块有哪些。
2. 每个模块到底能做什么。
3. 每个模块明确做不到什么，或者不适合拿来做什么。
4. 哪些能力是产品原生提供的，哪些能力需要你自己写胶水代码或配套基础设施。

## 2. 一句话总览

ClearML 的本质不是“自动帮你做机器学习决策”的系统，而是一个围绕机器学习代码执行过程构建的 **记录、复现、分发、编排、登记、展示** 平台。

它最擅长的是：

- 把一次脚本运行变成可追溯的 `Task`
- 自动记录参数、日志、指标、图表、模型、产物
- 把任务派发到远端机器执行
- 基于既有任务做批量克隆、调参、排队、编排
- 给数据集、模型、实验结果建立统一索引与展示界面

它不擅长也不负责的是：

- 帮你理解业务需求
- 自动设计特征工程策略
- 自动发明模型结构
- 替代 GitLab 做代码协作治理
- 替代 Spark/Flink 做实时数据处理
- 替代 Airflow/企业审批系统做复杂跨系统业务编排

## 3. ClearML 的平台边界

从官方文档和官方仓库来看，ClearML 可以拆成三层：

**3.1 运行记录层**


这一层负责回答“这次到底跑了什么”：

- `Task`
- `Logger`
- `Artifacts`
- `Models`
- `Console / Resource Monitoring`

**3.2 执行与自动化层**


这一层负责回答“这次在哪跑、怎么重跑、怎么批量跑”：

- `Agent`
- `Queues`
- `PipelineController`
- `PipelineDecorator`
- `HyperParameterOptimizer`
- `Scheduler`
- `Trigger`

**3.3 资产与展示层**


这一层负责回答“产物怎么登记、怎么查、怎么给别人看”：

- `Dataset`
- `Model Registry`
- `Reports`
- `Web UI`

下面按功能逐项展开。

## 4. Task：ClearML 的核心抽象

**4.1 它是什么**


`Task` 是 ClearML 最核心的对象。官方文档明确把它定义为一次代码执行会话，可以表示：

- 一次实验
- 一个工作流步骤
- 一个控制器任务
- 一个自定义后台服务

只要在代码里调用：

```python
from clearml import Task
task = Task.init(project_name="demo", task_name="train")
```

这次运行就会被纳入 ClearML 的可追踪体系。

**4.2 它能做什么**


`Task` 能做的事情非常多，几乎是所有功能的入口：

- 创建一条实验运行记录
- 绑定项目名、任务名、任务类型
- 自动采集代码仓库信息
- 自动采集命令行参数、部分配置
- 自动采集控制台输出
- 自动采集机器资源使用情况
- 承载手工上报的指标、图表、模型、工件
- 被克隆成新的实验副本
- 被加入队列，交给 Agent 远程执行
- 被 Pipeline / HPO / Scheduler 当作模板任务使用

**4.3 它不能做什么**


`Task` 本身不是：

- 代码版本管理系统
- 数据版本管理系统
- 工作流引擎本身
- 模型服务本身

更具体地说：

- 它能记录 Git 提交信息，但不能替代 `git commit` / `git push`
- 它能承载工件引用，但不等于自动把大数据治理好
- 它能被编排，但不等于你什么脚本都不用改就自动组成完美 DAG

**4.4 它容易被误解的地方**


最常见的误解有三个：

- **误解 1**：`Task.init()` 就等于“全自动 MLOps 已完成”
  实际上它只是把一次运行纳入可追踪体系，离自动化编排还差很多层。
- **误解 2**：有了 Task 就一定能完全复现
  实际上 Task 只能尽量记录运行信息，但前提仍然是代码仓库、依赖、数据路径本身是可复现的。
- **误解 3**：Task 能理解业务语义
  实际上它只知道“这次运行”以及其元数据，并不知道你的模型选择是否合理。

**4.5 前置条件**


要让 `Task` 真正有价值，至少需要：

- ClearML SDK 可用
- 配置好 `clearml-init`
- 代码最好在 Git 仓库中运行
- 参数和输出尽量结构化

**4.6 边界结论**


`Task` 是 ClearML 的最小工作单元，也是最值得优先理解的能力。它解决的是“记录和复现入口”，不是“智能决策”。

## 5. Logger：指标、图表、样本的统一上报接口

**5.1 它是什么**


`Logger` 是挂在 `Task` 上的结果上报接口，典型写法是：

```python
logger = task.get_logger()
```

官方文档把它的能力分为两类：

- 自动记录
- 手动记录

**5.2 它能做什么**


`Logger` 可以上报的内容非常丰富：

- 文本日志
- 标量序列
- 线图
- 柱状图
- 混淆矩阵
- 直方图
- 表格
- Plotly 图
- Matplotlib 图
- 图片
- 音频
- 视频
- HTML 内容

它适合承担“结果展示层”的职责。

**5.3 自动记录能覆盖到什么程度**


ClearML 的自动记录主要覆盖：

- `stdout` / `stderr`
- TensorBoard / TensorBoardX 输出
- Matplotlib 输出
- GPU / CPU / Memory / Network 资源监控

这意味着，如果你的训练脚本已经在写 TensorBoard 或画 Matplotlib 图，ClearML 往往可以直接接住这些输出。

**5.4 手动记录能覆盖到什么程度**


对于自动捕获不到的内容，可以手工上报，例如：

```python
logger.report_scalar("metric", "f1", value=0.91, iteration=1)
logger.report_table("eval", "summary", table_plot=df)
```

因此，`Logger` 的边界不是“只能记录官方支持的框架”，而是：

- 自动捕获的范围有限
- 手工上报的灵活度很高

**5.5 它不能做什么**


`Logger` 不能替代：

- 原始数据存储系统
- 模型仓库
- 长期大规模文件归档系统

尤其不适合把它当成“大文件存储接口”来使用。

官方文档也明确区分了：

- 调试样本适合走 `Logger`
- 需要后续消费的脚本产出更适合走 `Artifacts`

**5.6 典型边界**


以下做法通常是合理的：

- 上报损失曲线
- 上报评估表
- 上报混淆矩阵
- 上报若干张结果图

以下做法通常不合理：

- 每轮都上传大量原始明细文件
- 把超大视频或超大批量样本全都当调试样本塞进去
- 指望 Logger 替代数据仓库

**5.7 容易混淆的概念**


`task.connect()` 和 `logger.report_*()` 是两类能力：

- `task.connect()` 是把配置对象接入任务参数区
- `logger.report_*()` 是把结果写到可视化区

`connect()` 不负责自动生成训练曲线；训练曲线要么来自框架自动捕获，要么来自显式 `logger.report_*()`

## 6. Artifacts：任务级工件管理

**6.1 它是什么**


Artifacts 是绑定在某个 `Task` 上的任务产物，可以是：

- 文件
- 对象
- 表格
- 中间结果
- 路径引用

典型接口是：

```python
task.upload_artifact(name="features", artifact_object=obj)
```

**6.2 它能做什么**


Artifacts 适合做：

- 保存一次运行产生的中间结果
- 给下游任务提供明确输入引用
- 保存评估摘要
- 保存小到中型的结构化对象
- 让 Pipeline 中的步骤之间通过引用进行连接

**6.3 它不能做什么**


Artifacts 不适合承担完整数据湖职责。

尤其不适合：

- 管理 TB 级原始训练数据
- 频繁上传海量大文件
- 当成“什么都往里塞”的兜底仓库

**6.4 边界结论**


Artifacts 非常适合作为“任务级产物”和“流程节点之间的桥”，但不该被误用成全量数据管理方案。

## 7. Agent 与 Queues：远程执行与资源调度

**7.1 它们是什么**


Agent 是 ClearML 的执行器，Queue 是任务等待执行的地方。

官方文档对它的定义很清楚：

- Queue 是有序任务列表
- Agent 监听队列并拉取任务执行

**7.2 它们能做什么**


Agent 能做的事情包括：

- 从队列中取任务
- 拉取代码仓库
- 应用记录的未提交变更
- 创建运行环境
- 安装依赖
- 设置环境变量
- 运行脚本
- 回传日志和资源使用情况

Queue 能做的事情包括：

- 对任务排队
- 让多个 Agent 服务同一队列
- 让一个 Agent 监听多个队列
- 通过队列语义做资源隔离

**7.3 它们不能做什么**


Agent 不会自动解决以下问题：

- 你项目依赖没声明清楚
- 你代码写死了本地路径
- 你远程仓库权限没配好
- 你的数据盘在执行节点上根本不可见
- 你的系统包依赖不完整

也就是说，Agent 不是“脏环境修复器”，而是“把已有可复现条件落到执行机上的执行器”。

**7.4 动态能力边界**


官方文档说明 Agent 支持：

- Docker 模式
- virtualenv 模式
- conda 模式
- 服务模式
- GPU 分配

这说明 Agent 的边界并不只是“跑训练脚本”，它还能跑：

- Pipeline controller
- HPO controller
- 定时服务
- 触发器服务

但这并不意味着它适合把所有后台逻辑都塞进去。

**7.5 服务模式的边界**


官方文档特别强调 `services-mode` 适合：

- pipeline controller
- HPO
- scheduler
- 外部通知服务

不适合：

- 重训练任务
- 重推理任务

所以服务队列和训练队列应当明确区分。

**7.6 边界结论**


Agent 解决的是“任务怎么到机器上稳定执行”，不是“怎么自动整理你的工程烂摊子”。

## 8. Hyperparameter Optimization：批量调参与搜索策略

**8.1 它是什么**


ClearML 的 HPO 是基于“克隆一个模板任务，再用不同参数反复运行”的机制实现的。

官方提供的搜索策略包括：

- Optuna
- BOHB
- RandomSearch
- GridSearch
- 自定义 SearchStrategy

**8.2 它能做什么**


HPO 能做的事情很明确：

- 指定一个基础任务作为模板
- 定义超参搜索空间
- 设定目标指标
- 指定最大并发任务数
- 指定运行队列
- 启动一批实验
- 追踪每个实验结果
- 选择最优配置

**8.3 它不能做什么**


HPO 不会自动帮你：

- 发明新的模型家族
- 自动理解哪些特征应当加入
- 自动修正训练代码 bug
- 自动设计合理搜索空间

换句话说，HPO 自动化的是“搜参执行”，不是“研究策略生成”。

**8.4 隐含前提**


HPO 要有价值，至少要满足：

- 基础任务已经能稳定单次运行
- 要优化的参数是可参数化的
- 指标是明确可读取的
- 队列和算力足够支撑并发

如果模板任务本身都不稳定，HPO 只会把失败复制很多次。

**8.5 边界结论**


HPO 是 ClearML 很强的一项能力，但它依赖一个前提：你的单任务训练脚本已经被整理成“可克隆、可覆盖参数、可读指标”的模板。

## 9. PipelineController 与 PipelineDecorator：工作流编排

**9.1 它们是什么**


ClearML 提供两类主要流水线能力：

- `PipelineController`：基于已有任务模板编排
- `PipelineDecorator`：基于 Python 函数编排

**9.2 它们能做什么**


流水线能力可以完成：

- 定义步骤依赖关系
- 克隆已有任务作为步骤实例
- 为下游步骤覆写参数
- 指定步骤运行队列
- 追踪步骤状态
- 在 UI 里展示 DAG
- 支持回调、缓存、条件控制的一部分能力

**9.3 `PipelineController` 的边界**


`PipelineController` 更适合：

- 你已经有若干独立可运行的模板任务
- 希望通过任务级编排把它们串起来

它不适合：

- 你所有逻辑都耦合在一个大脚本里
- 你希望不拆模块就直接自动变成 DAG

**9.4 `PipelineDecorator` 的边界**


`PipelineDecorator` 更适合：

- 逻辑本来就写成清晰的 Python 函数
- 希望直接从函数层做流水线

它不适合：

- 大量返回超大对象
- 复杂跨语言步骤
- 强依赖外部系统事务控制的场景

**9.5 它们不能做什么**


ClearML Pipeline 不是：

- 企业级 BPM 系统
- 跨部门审批流系统
- 高复杂度数据调度中台
- 实时流式处理框架

它更适合的是“ML 任务级 DAG”，而不是“公司级业务流程引擎”。

**9.6 边界结论**


Pipelines 很强，但前提是你的步骤边界本身已经清楚。它编排的是“已经能独立站住的任务”，不是替你把混乱脚本自动模块化。

## 10. Scheduler 与 Trigger：定时与事件驱动自动化

**10.1 它们是什么**


ClearML 还提供两类轻量自动化控制器：

- `TaskScheduler`：按时间周期触发
- `TriggerScheduler`：按系统状态触发

仓库示例显示，触发条件可以来自：

- 模型事件
- 数据集事件
- 任务指标阈值

**10.2 它们能做什么**


Scheduler / Trigger 能做的事情包括：

- 定时重跑某个任务
- 定时运行某段函数逻辑
- 在模型带某个 tag 时触发后续动作
- 在数据集创建后触发重训练
- 在某任务指标超过阈值时触发下一步

**10.3 它们不能做什么**


它们不是完整的外部事件总线，也不是通用 CI/CD 平台。

它们不直接负责：

- 解析 GitLab Issue 语义
- 审批流程
- 复杂重试补偿机制
- 跨系统长事务编排

如果你想根据 GitLab MR、Issue、Webhook 做非常细的业务触发，通常仍然需要：

- GitLab CI
- 自定义 webhook 服务
- 自己写的胶水脚本

**10.4 边界结论**


Scheduler / Trigger 非常适合做 ClearML 内部对象驱动的轻自动化，但不应被误解为“全能企业流程引擎”。

## 11. Dataset：文件型数据集版本管理

**11.1 它是什么**


官方文档明确说明 `clearml-data` / `clearml.Dataset` 提供的是 **文件型数据管理方案**。

它强调两个目标：

- accessibility
- versioning

并且支持：

- 父子数据集继承
- 差异化存储
- 本地缓存
- 从任意机器拉取同一版本数据

**11.2 它能做什么**


Dataset 很适合：

- 给一个训练数据版本打快照
- 给一个评估集打不可变版本
- 记录某次特征生成后的文件集合
- 在多个执行机之间共享明确的数据版本
- 给数据建立 lineage

**11.3 它不能做什么**


Dataset 不适合被当成：

- 实时数据流系统
- 大规模表级事务型数据仓库
- 特征平台
- 在线特征服务

它是“文件数据版本化工具”，不是“全能数据平台”。

**11.4 它的关键成本**


Dataset 最大的边界不在功能，而在成本：

- 需要对象存储或共享存储
- 会带来上传、缓存、拉取成本
- 数据体量大时会带来明显 IO 压力
- 你需要改变原有读写范式

**11.5 边界结论**


Dataset 的价值主要在“追溯和复现”，不是“替代现有大数据基础设施”。如果你的数据治理已经由共享盘、Hive、Lakehouse 或其他平台承担，ClearML Dataset 不一定是第一优先级。

## 12. Model Registry：模型注册与血缘管理

**12.1 它是什么**


官方文档把 Model Registry 定义为：

- catalog and share models
- full traceability and provenance
- model lineage

核心思想不是“存一个文件”，而是“把模型和它的来源、版本、标签、任务历史关联起来”。

**12.2 它能做什么**


Model Registry 能做的事情包括：

- 自动或手动注册模型
- 追踪模型来自哪个任务
- 查看模型 lineage
- 给模型打标签
- 发布模型
- 让别人通过 model id 拉取模型
- 作为后续部署流程或 CI/CD 的输入源

**12.3 它不能做什么**


Model Registry 不等于：

- 高可用模型服务
- 模型治理委员会
- 线上灰度发布系统

它能登记模型、索引模型、追踪模型，但不自动负责线上部署策略。

**12.4 一个重要边界**


“模型被注册”不等于“模型就可用”。

模型真正可复用，还依赖：

- 文件存储可访问
- 模型格式规范
- 推理代码兼容
- 上下游知道如何消费这个模型

**12.5 边界结论**


Model Registry 解决的是“模型资产可追溯、可查找、可引用”，不是“模型自动上生产”。

## 13. Reports：结果汇总与展示

**13.1 它是什么**


Reports 是 ClearML 的文档化展示能力，用来把多个任务的图表、结果和说明汇总为统一页面。

**13.2 它能做什么**


Reports 适合：

- 做实验对比页面
- 做阶段性评估汇报
- 汇总多个任务的图表
- 输出可分享链接
- 作为结果展示层

**13.3 它不能做什么**


Reports 不是：

- 商业 BI 平台
- 文档协作平台
- 自动结论生成器

它能汇总和展示，但不保证“结论已经替你写好并永远正确”。

**13.4 边界结论**


Reports 是“展示层增强器”，不是“分析智能体”。它最有价值的地方是把实验结果标准化呈现出来。

## 14. ClearML 明确不替代的能力

这一部分非常重要，因为很多人第一次接触 ClearML 时容易把它想成“ML 自动化银弹”。

以下能力，ClearML 明确不替代：

**14.1 不替代业务理解**


它不负责：

- 理解甲方需求
- 解释业务目标
- 决定优化哪个指标

**14.2 不替代算法研究**


它不负责：

- 想出新特征
- 设计新模型
- 判断哪种策略更符合业务逻辑

**14.3 不替代代码协作平台**


它不负责：

- 分支治理
- 合并策略
- Code Review
- Issue 生命周期

这些仍然是 GitLab / GitHub 的职责。

**14.4 不替代通用数据平台**


它不负责：

- 流式数据处理
- 数仓建模
- 特征仓库
- 数据权限治理全栈

**14.5 不替代通用企业流程引擎**


它不负责：

- 审批流
- 多系统对账流
- 跨部门协同编排
- 通用业务自动化编排

## 15. 哪些能力是原生的，哪些要自己补

**15.1 ClearML 原生就有的**


- Task 记录
- Logger 上报
- Artifacts
- Agent / Queue
- HPO
- Pipeline
- Scheduler / Trigger
- Dataset
- Model Registry
- Reports

**15.2 需要你自己补胶水层的**


- GitLab Issue / MR 触发实验
- GitLab 评论回写
- 与企业内部权限系统打通
- 与已有数据平台对接
- 与已有报表系统对接
- 审批门禁

这部分很关键。很多“看起来像是 ClearML 应该支持”的流程，其实不是 ClearML 缺功能，而是属于“外部系统集成”问题。

## 16. 如果只从功能价值角度看，最值得优先理解哪些

如果单纯从“功能边界认知”而不是“实施优先级”来看，最应该先理解的是：

1. `Task`
2. `Logger`
3. `Artifacts`
4. `Agent + Queue`
5. `HPO`
6. `Pipeline`
7. `Dataset`
8. `Model Registry`

原因很简单：

- `Task` 决定 ClearML 的基本抽象
- `Logger / Artifacts` 决定结果怎么沉淀
- `Agent / Queue` 决定任务怎么远程执行
- `HPO / Pipeline` 决定自动化上限
- `Dataset / Model Registry` 决定资产是否真正可追溯

## 17. 简化到你的场景，只保留一句判断

对于 `data_pipeline + models + eval` 这类预测型项目，ClearML 最值得重视的能力边界不是“它能不能自动帮你做业务判断”，而是：

- 它非常擅长把你的现有 ML 脚本体系变成可记录、可复现、可排队、可批量运行、可展示的系统
- 它不擅长也不负责替你完成需求理解、策略设计、跨系统治理和复杂业务审批

## 18. 参考依据

本报告主要基于以下内容整理：

- ClearML 官方文档首页
- 官方 `Tasks` 文档
- 官方 `Logger` 文档
- 官方 `Workers & Queues` 文档
- 官方 `Hyperparameter Optimization` 文档
- 官方 `Pipeline from Tasks` 文档
- 官方 `ClearML Data` 文档
- 官方 `Model Registry` 文档
- 本仓库 `README.md`
- 本仓库 `examples/`
- 本仓库 `docs/`
